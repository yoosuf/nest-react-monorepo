# Monorepo (Nest JS + React JS + Vite )


```bash
# install all dependencies
$ yarn

# run the frontend app
$ yarn start:app

# run the server app
$ yarn start:server

# run both at once
$ yarn start
```